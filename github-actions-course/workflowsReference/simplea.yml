name : ShellCommands

on : push

jobs:
  run-shell-command:
    runs-on : ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hellow Git hub actions"
      - name: multiline
        run: |
          node -v
          npm -v